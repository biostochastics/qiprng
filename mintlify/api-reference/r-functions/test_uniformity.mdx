---
title: 'test_uniformity'
description: 'Test uniformity using Kolmogorov-Smirnov and Chi-squared tests'
sidebarTitle: 'test_uniformity'
---

# Test uniformity using Kolmogorov-Smirnov and Chi-squared tests


 Performs comprehensive uniformity testing on a sample of random numbers using
 both the Kolmogorov-Smirnov test and Chi-squared goodness-of-fit test.

## Usage

```r


test_uniformity(samples, bins = 20)
```

## Parameters

<ParamField body="samples" type="any">
  Numeric vector of random samples to test
</ParamField>

<ParamField body="bins" type="any">
  Number of bins for the Chi-squared test (default: 20)
</ParamField>

## Returns


- A list containing test results:

 test_name Name of the test suite
 ks_statistic Kolmogorov-Smirnov test statistic
 ks_p_value Kolmogorov-Smirnov p-value
 chi_sq_statistic Chi-squared test statistic
 chi_sq_p_value Chi-squared p-value
 passed Logical; TRUE if both tests pass at 0.05 significance
 interpretation Human-readable interpretation of results


## Examples

## Details


- The function performs two complementary tests:
-
- Kolmogorov-Smirnov test : Compares the empirical CDF against uniform 0,1
- Chi-squared test : Tests if bin frequencies match expected uniform distribution
-
-
- A warning is issued if expected bin counts are less than 5, as this may
- affect the validity of the Chi-squared test.
